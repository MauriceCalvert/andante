do_re_mi is defined in schemas.yaml.
it is duplicated in core.yaml which appears to be dedicated to invention.
this violates law L017 and is confusing as 'core' suggests universality

schema.yaml is used to by schema_validator to ensure coherence amongst yaml files.
this is confusing because the term 'schema' is a musical concept.
rename to make this clear.
enhance schema_validator to cross-check all yaml files in the project, strictly and exhaustively,
it must also produce a yaml_usages file which shows which source modules use which yaml files.

cadence_formulas.yaml should be merged into cadences.yaml.

_TONAL_ROOTS and TONAL_ROOTS are duplicate definitions in 3 different files. fix this and similar duplications of constants

'key' (tonic and mode) and 'tonic' are mis-used. variables names etc called 'key' that refer to diatonic concepts should be 'tonic'

when key is omitted, does the planner choose key according to mattheson (Part VIII: Affect and Expression 8.1 Affektenlehre in @file:///D:/projects/Barok/barok/source/andante/docs/Tier4_Reference/baroque_theory.md? if not, it must

treatments.yaml. values like soprano_source: subject and bass_source: subject are specified repeatedly, would it make sense to have a '_default' treatment at the top?


arc_selction.yaml, counterpoint_rules.yaml, devices.yaml, embellishments.yaml, energy.yaml,
episodes.yaml, figures.yaml, gestures.yam, lessons.yaml,ornaments.yaml,positions.yaml ,
predicates.yaml,rhythms.yaml,section_templates.yaml,surprises.yaml,textures.yaml,
voice_spacing.yaml,walking_patterns.yaml
seem orphaned but contain usefuil information. duplication? unify with others? keep for refernce but move?